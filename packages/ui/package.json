{
  "name": "@ghumle/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean --external react --external react-dom",
    "dev": "tsup src/index.ts --watch --format esm,cjs --dts --external react --external react-dom",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('No tests configured for @ghumle/ui')\""
  },
  "dependencies": {
    "@ghumle/design-tokens": "0.1.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.5.0"
  },
  "peerDependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
